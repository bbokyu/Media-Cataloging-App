extends ../layout.pug

block content
    div.profile-page
        h1(class="h1 mb-0") #{user.user}'s Profile

        a(href='/user/updateUser') Update Account
        br
        a(href='/user/deleteUser') Delete Account
